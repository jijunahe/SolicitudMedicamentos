# URL del backend para el proxy en desarrollo (npm run dev)
# Por defecto: http://localhost:8000
VITE_PROXY_TARGET=http://localhost:8000

# Base URL de la API para las peticiones desde el navegador (por defecto /api)
VITE_API_URL=/api
